# Verified by requesting https://api.moonshot.ai/v1/models: the endpoint answers and speaks
# the OpenAI wire format. Models are discovered with `confai provider sync`
# rather than listed here, so this file cannot go stale.

id = "moonshot"
name = "Moonshot (Kimi)"
description = "Kimi models, international endpoint"
homepage = "https://platform.moonshot.ai/docs"
api_key_env = "MOONSHOT_API_KEY"

[provider]
base_url = "https://api.moonshot.ai/v1"
wire_api = "chat"
