# Verified by requesting https://api.together.xyz/v1/models: the endpoint answers and speaks
# the OpenAI wire format. Models are discovered with `confai provider sync`
# rather than listed here, so this file cannot go stale.

id = "together"
name = "Together AI"
description = "A broad catalogue of open-weight models"
homepage = "https://docs.together.ai/"
api_key_env = "TOGETHER_API_KEY"

[provider]
base_url = "https://api.together.xyz/v1"
wire_api = "chat"
