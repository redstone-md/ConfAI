# Verified by requesting https://inference.baseten.co/v1/models: the endpoint answers and speaks
# the OpenAI wire format. Models are discovered with `confai provider sync`
# rather than listed here, so this file cannot go stale.

id = "baseten"
name = "Baseten"
description = "Open-weight models on dedicated deployments"
homepage = "https://docs.baseten.co/"
api_key_env = "BASETEN_API_KEY"

[provider]
base_url = "https://inference.baseten.co/v1"
wire_api = "chat"
