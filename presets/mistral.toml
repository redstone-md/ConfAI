# Verified by requesting https://api.mistral.ai/v1/models: the endpoint answers and speaks
# the OpenAI wire format. Models are discovered with `confai provider sync`
# rather than listed here, so this file cannot go stale.

id = "mistral"
name = "Mistral"
description = "Mistral direct"
homepage = "https://docs.mistral.ai/"
api_key_env = "MISTRAL_API_KEY"

[provider]
base_url = "https://api.mistral.ai/v1"
wire_api = "chat"
